# Otto — releases

Update feed and downloads for Otto, a personal Mac assistant.

`appcast.xml` is what the app reads to find new versions. Downloads are signed;
Otto refuses any that don't verify. The source lives in a separate private repo.
